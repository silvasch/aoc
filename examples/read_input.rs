use aoc::{read_example_input, read_example_output, read_input};

fn main() {
    dbg!(read_input(1).unwrap());
    dbg!(read_example_input(1, 1).unwrap());
    dbg!(read_example_output::<u32>(1, 1).unwrap());
}
